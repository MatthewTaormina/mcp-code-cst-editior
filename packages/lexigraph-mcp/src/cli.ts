#!/usr/bin/env node
// Lexigraph MCP server entry point.
// Phase 4 will wire @modelcontextprotocol/sdk + tools.
console.error('lexigraph-mcp: scaffold; server not yet implemented (see AGENTS.md, Phase 4).');
process.exit(1);
